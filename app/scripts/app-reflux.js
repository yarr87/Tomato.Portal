var Router = require('router');

// Reflux app bootstrap - new app.js is redux
//Router.start();

require('jQuery');
require('bootstrap');

// Global react object for non-browserify plugins
//var React = require('react');
//window.React = React;